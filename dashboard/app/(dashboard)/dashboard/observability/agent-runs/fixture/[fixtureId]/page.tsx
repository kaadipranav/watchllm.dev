/**
 * Agent Run Debug Page - Fixture Route
 * 
 * This page uses the same component as the regular debug page
 * but loads fixture data instead of database data.
 * 
 * @feature AGENT_DEBUGGER_V1
 */

export { default } from '../[runId]/debug/page';
